# Development Notes

## Linting and Typechecking
- Run `swiftlint` to lint the code.
- Run `swift build` to typecheck and build.

## Testing
- Run `swift run` to launch the app.
- Ensure Google Chrome is running with YouTube tabs for testing detection.